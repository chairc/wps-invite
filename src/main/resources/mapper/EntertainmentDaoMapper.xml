<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.wps.wps_invite.mapper.EntertainmentDao">
    <!--关于ResultMap 如果实体类与数据库的行相同则不用写，反之则需要写ResultMap-->
    <sql id="table">数据库中表名</sql>

    <select id="getWpsSidAll" resultType="com.example.wps.wps_invite.model.Entertainment">
        SELECT
        *
        FROM
        <include refid="table"/>
    </select>

</mapper>